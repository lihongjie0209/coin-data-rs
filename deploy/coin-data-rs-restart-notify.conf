[Service]
# Run systemctl with full privileges and return immediately. The Telegram request
# is handled by a separate constrained oneshot service, outside the collector.
ExecStartPost=+/usr/bin/systemctl --no-block start coin-data-restart-notify.service

